# 失效 / 断链清单

> 部署区**唯一维护**（权威稿）。前端若有副本，以本文件为准。扫描日：2026-09-11。  
> 范围：`部署/docs/`、`部署/kb/GALNAVI-KB/`。  
> **不删除** `05_Log/Problem/` 正文，除非另行确认过时。

对照根：本区有 `docs/`、`kb/`、`wrangler/`、`temp/`、空接收区 `worker/new/`、开源副本 `gal-navigation/`。  
**本区无** `src/`、`sandbox/`、`source/`（在 `前端/`）。

状态图例：

| 标记 | 含义 |
|------|------|
| 仍有效 | 口径/教训仍成立；路径可能需跨区理解 |
| 部署区断链 | 相对本区打开会找不到文件；内容本身未废 |
| 已过时（待改） | 描述的是拆分前单体仓或已完成的整改项 |
| 警示保留 | Problem 类；记录索引写「无 docs 对应」属预期 |

---

## 1. 真·Markdown 相对断链（本区打开即 404）

| 文件 | 链接 | 状态 | 说明 |
|------|------|------|------|
| `docs/README.md` | → `../../前端/src/README.md` | **已修 2026-09-11** | 指向前端区 |
| `docs/examples/README.md` | → `../../../前端/src/preview/index.html` | **已修 2026-09-11** | 指向前端区 |

**处理建议（未改原文）**：改为指向 `../../前端/src/...`，或加「源在前端工作区」说明句；改文档时走总指挥对齐。

---

## 2. 路径引用：`src/` / `src/preview`（大量，部署区无实体）

下列为**路径字面量/文档叙述**，非全部是可点击相对链接。在部署区单独打开会「找不到」；在前端区仍有效。

| 区域 | 代表文件 | 状态 | 备注 |
|------|----------|------|------|
| docs ADR | `0002` `0004` `0009` | 仍有效 + 部署区断链 | 讲构建/内联原理，路径应读作前端 `src/` |
| docs standard | `tokens.md` `usage.md` | 同上 | 换肤入口、esbuild 命令 |
| docs | `components.md` `ARCHITECTURE.md` `CHANGELOG.md` `README.md` | 同上 | 组件路径索引 |
| kb 02_Component | 几乎全部组件篇 + `GD 组件库.md` | 同上 | 源码路径均写 `src/...` |
| kb 05_Log | `记录索引.md`「活示例：src/preview/...」 | 部署区断链 | 应注明前端区 |

**处理建议**：在 `docs/README.md` / kb 入口加一句「组件源码与预览在 `前端/`，本区 docs 路径按逻辑仓书写」；不必批量改历史 CHANGELOG。

---

## 3. Problem 与记录索引

| 条目 | 状态 | 说明 |
|------|------|------|
| [[Problem-公告注入布局错乱]] | **警示保留 / 仍有效** | `status: resolved`；教训（整块替换 DOM）仍有效。记录索引「无 docs 对应」= 仅 kb，**不是失效** |
| [[Problem-shenmo 命名混乱]] | **警示保留 / 仍有效** | 已 resolved；2026-09-01 注明 `src/extend/palace/` 已删。防旧名回流仍有用 |
| `记录索引.md` → Problem 节标题 | **已修 2026-09-11** | 现为「仅 kb，无 docs 镜像」 |

**本轮：未删、未改 Problem 正文。**

---

## 4. `open-source-prep.md` 与拆分后结构

| 条目 | 状态 | 说明 |
|------|------|------|
| 敏感信息：status token、`.wrangler`、真实 D1/KV ID | **仍有效** | 开源红线不变；真实 ID 仍在本区 `wrangler/*.toml` |
| 「可删除 backup/sandbox-freeze/…」 | **已废弃（open-source-prep 已更新）** | 拆分后不再当待办 |
| toml 整改表（help/galnavi/about…） | **已废弃（open-source-prep 已更新）** | 现网 toml 已对齐 `worker/new` |
| 开源文件清单（单体仓） | **已废弃（open-source-prep 已按拆分重写）** | 见新版第二节 |
| `source/` 保留、status 不抽 | **仍有效** | 口径仍对；本区尚未建 `source/` |

---

## 5. kb 部署/沙盒叙述 vs 目录拆分

| 文件 | 状态 | 说明 |
|------|------|------|
| `04_Internal/Deployment/部署流程.md` | **仍有效** | toml→`worker/new` 口径对；接收目录在本区，源交付自前端 |
| `04_Internal/Deployment/沙盒开发.md` | **仍有效 + 部署区无 sandbox** | 沙盒在前端；文中 `node sandbox/serve.mjs` 需在前端区执行 |
| `03_Architecture/Worker 架构.md` | **仍有效** | 已区分现网 `worker/new` vs GitHub `gal-navigation/worker/` |
| `01_Project/*` 目录表 | **仍有效（跨区）** | 写成「完整工程」视角；拆分后要心里映射到前端/部署 |

---

## 6. 本区 `worker/new` 现状

- 目录已建，目前仅占位 `README.md`，**尚无**十页 `.js` 副本。
- `wrangler` 的 `main` 已正确指向本区 `../worker/new/*.js`。
- 发版前须经总指挥收前端交付并拷入，否则 deploy 会缺文件（配置有效、产物未落盘）。

---

## 7. 已执行 / 仍待定

1. ~~修 §1 两链~~ **已做**（指向 `前端/src/...`）
2. ~~修订 open-source-prep~~ **已做**（拆分后结构；过时清单标废弃）
3. ~~记录索引标题措辞~~ **已做**
4. docs 入口跨区说明：**已加**（`docs/README.md`）
5. ADR/CHANGELOG 的 `src/` 叙述：**仍不批量改**（总指挥拍板）
6. **不**删 Problem 正文

---

## 扫描命令备忘

路径扫描与相对链接检查脚本可再放 `temp/`；本清单正文维护在 `docs/standard/失效断链清单.md`。

